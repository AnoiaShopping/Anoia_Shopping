/*package dam.anoiashopping.gtidic.udl.cat.utils;

import dam.anoiashopping.gtidic.udl.cat.R;

public class Validation {

    //TODO

    public static ValidationResultImpl isValidUsername (String username) {

        String msg = "";
        boolean isValid = true;

        getUsernameError (msg, isValid, username);

        return new ValidationResultImpl(msg, isValid);
    }

    public static void getUsernameError (String msg, boolean isValid, String username) {

        if (username.isEmpty()) {
            msg = getString(R.string.emptyUsername);
            isValid = false;
            return;
        }


    }
}*/
