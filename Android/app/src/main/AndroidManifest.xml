<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dam.anoiashopping.gtidic.udl.cat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:icon="@mipmap/anoiashoppinglogo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/anoiashoppinglogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android"
        tools:targetApi="n">
        <activity android:name=".views.UserEditActivity" />
        <activity android:name=".views.SearchUserActivity" />
        <activity android:name=".views.ConfigurationActivity" />
        <activity android:name=".views.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Buscar solució per el onbackpressed -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.RegisterConfirmationActivity" />
        <activity
            android:name=".views.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
        </activity>
        <activity android:name=".views.RegisterActivity" />
    </application>

</manifest>